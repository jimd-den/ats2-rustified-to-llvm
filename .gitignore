/target

# The upstream ATS2 compiler, cloned in for its example corpus.  It is
# someone else's repository and 108MB of it, so it is fetched rather than
# vendored:
#
#     git clone https://github.com/githwxi/ATS-Postiats.git
#
# scripts/score-corpus.sh reads ATS-Postiats/doc/EXAMPLE/INTRO.
/ATS-Postiats

# Abandoned scaffolding from an earlier start: a Cargo.toml and a
# three-line main.rs, with a git repo of its own and no commits.  Not a
# member of the workspace.
/ats2_rustified
ats2-harness-failures.csv
ats2-harness-ir.txt
